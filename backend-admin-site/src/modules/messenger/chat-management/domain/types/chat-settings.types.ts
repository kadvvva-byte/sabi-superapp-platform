export interface ChatSettingsDTO {
  chatId: string
  userId: string
}

export interface PinChatDTO {
  chatId: string
  userId: string
}

export interface MuteChatDTO {
  chatId: string
  userId: string
}

export interface ArchiveChatDTO {
  chatId: string
  userId: string
}