export class SabiCallRoomRegistry { private readonly rooms = new Map<string, Set<string>>(); join(callId: string, socketId: string): void { if (!this.rooms.has(callId)) this.rooms.set(callId, new Set()); this.rooms.get(callId)?.add(socketId); } leave(callId: string, socketId: string): void { this.rooms.get(callId)?.delete(socketId); } list(callId: string): string[] { return Array.from(this.rooms.get(callId) ?? []); } }
