export class SabiCallUserSocketRegistry { private readonly sockets = new Map<string, Set<string>>(); bind(userId: string, socketId: string): void { if (!this.sockets.has(userId)) this.sockets.set(userId, new Set()); this.sockets.get(userId)?.add(socketId); } unbind(userId: string, socketId: string): void { this.sockets.get(userId)?.delete(socketId); } list(userId: string): string[] { return Array.from(this.sockets.get(userId) ?? []); } }
