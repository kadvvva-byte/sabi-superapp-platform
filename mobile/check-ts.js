const ts=require('typescript');const fs=require('fs');const files=process.argv.slice(2);for(const f of files){const s=fs.readFileSync(f,'utf8');const r=ts.transpileModule(s,{compilerOptions:{jsx:ts.JsxEmit.React,module:ts.ModuleKind.CommonJS,target:ts.ScriptTarget.ES2020}});if(r.diagnostics&&r.diagnostics.length){console.error(r.diagnostics.map(d=>d.messageText).join('\n'));process.exit(1);}console.log('transpile ok',f);}